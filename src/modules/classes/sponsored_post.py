from modules.classes.post import Post

class SponsoredPost(Post): # Postクラスを継承
    pass
    # def __init__(self, title, content, author, sponsor):
    #     super().__init__(title, content, author)
    #     self.sponsor = sponsor

    # def show_post(self):
    #     return f"{self.title} by {self.author} (Sponsored by {self.sponsor})"